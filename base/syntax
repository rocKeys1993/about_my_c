1. C 是大小写敏感的。。。
2. int a ; 和 int a , b , c ;//若没有变量初始化，则字符类型应该会被初始化为null，数值类型为0
3. 关于变量:
	声明和定义：
	1. 声明 extern 用来声明变量，如果exter 变量的同时，初始化，则会报warning的错误。
	2. 暂时，把变量的声明理解为给变量名分配一块寄存器资源，该变量名或许就是指针，声明的变量名被编译之后，就是一个坐标，至于分配寄存器的单位，需要给该变量定义。
	3. 也就是说，声明，或许就是变量坐标，定义就是变量空间。

	关于extern令牌：
		extern a ,b ; //此时不需要声明a,b的数据类型！compile 会报 warning，但是不影响程序编译和执行。
		extern int a , b ; // 此时编译不会报错，如果在函数内使用extern令牌，则在全局定义中一定已经有一个变量声明了。
		int a ,b  和 extern int a,b 的区别：
			int a,b ; // 假设该声明位于函数内，则不论是否有a,b的全局变量，都会将该作用域中的a,b变量初始化。
			extern int a,b; // 假设该声明位于函数内，则会取判断是否有a,b 的全局变量，如果有，则直接使用


	

函数入口：
  main函数，似乎必须返回int型

关键字
  \n 换行符


转义符
  \ 
