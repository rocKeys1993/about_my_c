#define S(a,b) a*b
#include <stdio.h>

int define_test()
{
    int a = 0;
    int b = 1;
    int result = S(a,b);
    return result;
}

int main()
{
    printf("define define_test number is : %d",define_test());
}
